import numpy as np
from array import array as ar

'''result = 0
def lineal (number):
    result = 0
    for x in range(0, number):
        ++result
    return result
print(result)

x = list(np.random.randint(low=1, high=50000, size=9999999))

def constante(x:list)-> list:
    return x

constante(x)

def iterador_dividido(x1:list) ->list:
    iterador = -len(x1)
    incremento = 1
    while(iterador < 0):
         iterador /= incremento
         incremento += 1
    return x1

iterador_dividido(x1)'''

'''def buscar(lista, nombre_buscado):
    tamano_lista = len(lista)
    for actual in range(0, tamano_lista):
        if (lista[actual]==nombre_buscado):
            return actual
        return -1
    def main():
        lista_alumnos = sorted(importa_lista('./data_reto1.xlsx'))'''

